import pytest
from backend.index_builder import load_emails_as_documents

def test_load_emails_as_documents_returns_list():
    pass
    # docs = load_emails_as_documents()
    # assert isinstance(docs, list)
    # # Documents should have text and metadata attributes
    # if docs:
    #     doc = docs[0]
    #     assert hasattr(doc, 'text')
    #     assert hasattr(doc, 'metadata')
